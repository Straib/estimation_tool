name: estimation_tool
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  auto_route: ^11.1.0
  flutter:
    sdk: flutter
  flutter_rearch: ^1.7.2
  google_fonts: ^6.2.1
  http: ^1.3.0
  rearch: ^1.16.1
  web_socket_channel: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.13.1
  auto_route_generator: ^10.5.0

flutter:
  uses-material-design: true
