name: estimation_tool_server
description: Session backend for the estimation tool.
publish_to: none
version: 0.1.0
uses-material-design: true

environment:
  sdk: ^3.7.2

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_web_socket: ^3.0.0
  web_socket_channel: ^2.4.0

dev_dependencies:
  lints: ^6.0.0

executables:
  estimation_tool_server: bin/server.dart
