import 'package:flutter/material.dart';

class ConsensusDisplay extends StatelessWidget {
  const ConsensusDisplay({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}